apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mycharts.fullname" . }}-test-connection"
  labels:
{{ include "mycharts.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "mycharts.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
