apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "app3.fullname" . }}-test-connection"
  labels:
{{ include "app3.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "app3.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
